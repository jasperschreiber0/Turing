# Root railway.toml — monorepo with two services.
# Each service overrides this via its own service definition in Railway dashboard.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "$SERVICE_START_CMD"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
